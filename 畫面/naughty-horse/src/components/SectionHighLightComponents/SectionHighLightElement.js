import styled from "@emotion/styled";

export const ImageBox = styled.div({
  display: 'flex',
  width: '100%',
  height: '100%'
});

export const ImageItem = styled.img({
  width: '90%',
  margin: 'auto auto 0',
});

export const SectionHighLightItem = styled.div({
  display: 'flex',
  width: '85%',
  height: '100%',
  margin: 'auto',
})